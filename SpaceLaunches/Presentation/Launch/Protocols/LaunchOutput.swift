/// Protocol defining the output produced by a `LaunchViewModel`.
///
/// `LaunchOutput` serves as an abstraction for all possible outcomes or data that a ViewModel
/// associated with a launch screen or feature can produce.
protocol LaunchOutput { }
