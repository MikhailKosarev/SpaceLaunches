/// Protocol defining the input expected by a `LaunchViewModel`.
///
/// `LaunchInput` serves as an abstraction for all possible input data or actions that can
/// be provided to a ViewModel associated with a launch screen or feature.
protocol LaunchInput { }
