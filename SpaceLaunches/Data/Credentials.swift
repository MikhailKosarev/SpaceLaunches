import Foundation

/// Credentials for different APIs.
struct Credentials {
    /// Credentials for Launch Library 2 API.
    struct LaunchLibrary2 {
        /// The base URL for Launch Library 2 API.
        static let baseURL = URL(string: "https://lldev.thespacedevs.com/2.2.0")!
    }
}
