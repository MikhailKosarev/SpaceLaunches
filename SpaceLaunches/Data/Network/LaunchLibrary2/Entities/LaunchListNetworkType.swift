/// Type of launch lists to get from the network.
enum LaunchListNetworkType: String {
    /// The upcoming launch list.
    case upcoming = "upcoming"
    /// The previous launch list.
    case previous = "previous"
    /// All launches.
    case all = ""
}
